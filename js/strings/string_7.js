/* Create a variable greeting and assign it the value "Hello, World!".
Convert the string to:
Uppercase using .toUpperCase().
Lowercase using .toLowerCase().
Print both results using console.log().
Example output:
"HELLO, WORLD!"
"hello, world!" */
let greeting = "Hello, World!";
let stringToUpperCase = greeting.toUpperCase();
let stringToLowerCase = greeting.toLowerCase();

console.log(`${stringToUpperCase}\n${stringToLowerCase}`);
